import streamlit as st

pg = st.navigation([
    st.Page("home.py", title="Home"),
    st.Page("about.py", title="About"),
    st.Page("contact.py", title="Contact")
])

st.markdown("""
<style>
/* --- Header Bar --- */
header[data-testid="stHeader"] {
    background-color: #003366;   /* dark blue */
    height: 60px;
    display: flex;
    align-items: stretch;        /* let children fill full height */
    padding: 0 !important;
}

/* --- Text color --- */
header[data-testid="stHeader"] * {
    color: #ffffff !important;
}

/* --- Base navigation links (inactive) --- */
a[data-testid="stTopNavLink"] {
    background-color: transparent;
    color: #ffffff !important;
    border-radius: 0 !important;         /* remove rounding */
    height: 100% !important;             /* full header height */
    padding: 0 20px !important;          /* spacing */
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    text-decoration: none !important;
    transition: background-color 0.3s ease;
}

/* --- Hover effect --- */
a[data-testid="stTopNavLink"]:hover {
    background-color: #004080 !important;
}

/* --- Active (selected) page link --- */
a[data-testid="stTopNavLink"][aria-current="page"] {
    background-color: #0059b3 !important; /* active blue */
    color: #ffffff !important;
    font-weight: bold !important;
    border-radius: 0 !important;          /* ensure flat edges */
    height: 100% !important;
}
</style>
""", unsafe_allow_html=True)

pg.run()
