import streamlit as st

pg = st.navigation([
    st.Page("home.py", title="Home"),
    st.Page("about.py", title="About"),
    st.Page("contact.py", title="Contact")
])

st.markdown("""
<style>
/* --- HEADER BAR --- */
header[data-testid="stHeader"] {
    background-color: #003366;     /* dark blue */
    height: 60px !important;
    display: flex !important;
    align-items: stretch !important;
    padding: 0 !important;
}

/* --- RC-OVERFLOW CONTAINER --- */
header[data-testid="stHeader"] .rc-overflow {
    display: flex !important;
    align-items: stretch !important;
    height: 100% !important;
}

/* --- Make every nav link full-height, flat --- */
header[data-testid="stHeader"] .rc-overflow a[data-testid="stTopNavLink"] {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    height: 100% !important;           /* full height of header */
    padding: 0 24px !important;
    border-radius: 0 !important;       /* flat edges */
    background-color: transparent;
    color: #ffffff !important;
    text-decoration: none !important;
    transition: background-color 0.3s ease;
}

/* --- Hover --- */
header[data-testid="stHeader"] .rc-overflow a[data-testid="stTopNavLink"]:hover {
    background-color: #004080 !important;
}

/* --- Active Page (selected) --- */
header[data-testid="stHeader"] .rc-overflow a[data-testid="stTopNavLink"][aria-current="page"] {
    background-color: #0059b3 !important;
    color: #ffffff !important;
    font-weight: bold !important;
    border-radius: 0 !important;
    height: 100% !important;
}
</style>
""", unsafe_allow_html=True)

pg.run()
