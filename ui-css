import streamlit as st

pg = st.navigation([
    st.Page("home.py", title="Home"),
    st.Page("about.py", title="About"),
    st.Page("contact.py", title="Contact")
])

st.markdown("""
<style>
/* --- HEADER BAR --- */
header[data-testid="stHeader"] {
    background-color: #003366;   /* dark blue */
    height: 60px !important;
    display: flex !important;
    align-items: stretch !important;   /* allow children to fill full height */
    padding: 0 !important;
}

/* make all containers inside header stretch vertically */
header[data-testid="stHeader"] > div,
header[data-testid="stHeader"] nav,
header[data-testid="stHeader"] nav > div {
    display: flex !important;
    align-items: stretch !important;
    height: 100% !important;
}

/* --- TEXT COLOR --- */
header[data-testid="stHeader"] * {
    color: #ffffff !important;
}

/* --- BASE NAV LINKS --- */
a[data-testid="stTopNavLink"] {
    background-color: transparent;
    color: #ffffff !important;
    border-radius: 0 !important;
    height: 100% !important;         /* fill header height */
    display: flex !important;
    align-items: center !important;  /* vertical center */
    justify-content: center !important;
    padding: 0 24px !important;
    text-decoration: none !important;
    transition: background-color 0.3s ease;
}

/* --- HOVER EFFECT --- */
a[data-testid="stTopNavLink"]:hover {
    background-color: #004080 !important;
}

/* --- ACTIVE PAGE LINK --- */
a[data-testid="stTopNavLink"][aria-current="page"] {
    background-color: #0059b3 !important;
    color: #ffffff !important;
    font-weight: bold !important;
    border-radius: 0 !important;
    height: 100% !important;
}

/* --- Ensure nav container itself has no padding/margins --- */
[data-testid="stTopNav"] {
    height: 100% !important;
    display: flex !important;
    align-items: stretch !important;
    margin: 0 !important;
    padding: 0 !important;
}
</style>
""", unsafe_allow_html=True)

pg.run()
